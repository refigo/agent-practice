# Copy to `.streamlit/secrets.toml` for local Streamlit secrets,
# or paste the values into the Streamlit Cloud dashboard's Secrets editor.
# Do NOT commit the real secrets.toml.

OPENAI_API_KEY = "sk-..."
